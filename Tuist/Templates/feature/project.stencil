import ProjectDescription
import ProjectDescriptionHelpers

let project = Project.makeProject(
    name: "{{ name }}Feature",
    targetKinds: .feature,
    dependencies: [
        .featureDependency
    ]
)
