import Foundation

import Domain
import FeatureDependency

import RxSwift
import ReactorKit

public final class {{ name }}Reactor: Reactor {
    public var initialState = State()
    
    public init() { }
}

extension {{ name }}Reactor {
    public struct State {
    }
    
    public enum Action {
    }
    
    public enum Mutation {
    }
}
