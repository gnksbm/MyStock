import UIKit

import FeatureDependency

import RxSwift

public final class {{ name }}ViewController: BaseViewController<{{ name }}Reactor> {
    public override func bindState(reactor: {{ name }}Reactor) {
        let state = reactor.state.share()
    }
    
    public override func bindAction(reactor: {{ name }}Reactor) {
    }
    
    public override func configureLayout() {
    }
}
